---
/** Renders a JSON-LD object into a <script type="application/ld+json"> tag. */
interface Props {
  data: Record<string, unknown>;
}
const { data } = Astro.props;
---
<script type="application/ld+json" is:inline set:html={JSON.stringify(data)} />
